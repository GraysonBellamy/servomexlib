---
description: servomexlib.protocol — ProtocolKind, the ProtocolClient interface, AUTO detection, and the continuous + Modbus clients.
---

# `servomexlib.protocol`

ProtocolKind, the ProtocolClient interface, AUTO detection, and the continuous + Modbus clients.

::: servomexlib.protocol
